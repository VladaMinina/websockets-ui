{
  "compilerOptions": {
    "target": "ES2015",      // You can also use ESNext or ES2020
    "module": "ESNext",      // Set this to ESNext for ES modules
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true      // Optional, but can speed up compilation
  },
  "include": ["ts-implementation/**/*.ts"],
  "exclude": ["node_modules"]
}
